@model IEnumerable<Employee>

@{
    ViewData["Title"] = "الموظفين";
}


    <div class="row mt-2">
        <div class="col-12">
            <h1 class="text-center mb-2">بيانات الموظفين</h1>
            <a class="btn btn-success mb-2" asp-action="Add">إضافة موظف</a>
            <div class="table-responsive">
                <table class="table table-bordered table-hover">
                    <thead>
                        <tr>
                            <th class="text-center fs-6">الرقم القومي</th>
                            <th class="text-center fs-6">اسم الموظف</th>
                            <th class="text-center fs-6">العنوان</th>
                            <th class="text-center fs-6">رقم التليفون</th>
                            <th class="text-center fs-6">النوع</th>
                            <th class="text-center fs-6">الجنسية</th>
                            <th class="text-center fs-6">تاريخ الميلاد</th>
                            <th class="text-center fs-6">تاريخ التعاقد</th>
                            <th class="text-center fs-6">القسم</th>
                            <th class="text-center fs-6">الراتب</th>
                            <th class="text-center fs-6">موعد الحضور</th>
                            <th class="text-center fs-6">موعد الإنصراف</th>
                            <th class="text-center fs-6">العمليات</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var emp in Model)
                        {
                            <tr id="id-@emp.NationalID">
                                <td class="text-center">@emp.NationalID</td>
                                <td class="text-center">@emp.Name</td>
                                <td class="text-center">@emp.Address</td>
                                <td class="text-center">@emp.PhoneNumber</td>
                                <td class="text-center">@emp.Gender</td>
                                <td class="text-center">@emp.Nationality</td>
                                <td class="text-center">@emp.Birthdate.ToString("dd/MM/yyyy")</td>
                                <td class="text-center">@emp.DateOfContract.ToString("dd/MM/yyyy")</td>
                                <td class="text-center">@emp.Department.Name</td>
                                <td class="text-center">@emp.Salary</td>
                                <td class="text-center">@emp.AttendanceTime.ToString("h:mm tt")</td>
                                <td class="text-center">@emp.DepartureTime.ToString("h:mm tt")</td>
                                <td class="text-center">
                                    <a asp-action="Detailes" asp-route-id="@emp.Id" class="bi bi-binoculars-fill text-info"></a>
                                    <a asp-action="Edit" asp-route-id="@emp.Id" class="bi bi-pencil-square text-warning"></a>
                                    <i style="cursor:pointer" onclick="DeleteEmployee(@emp.NationalID)" class="bi bi-trash3-fill text-danger"></i>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>


            </div>
        </div>
    </div>
@section Scripts{
    <partial name="_ValidationScriptsPartial" />
    <script>
        function DeleteEmployee(id) {
            $.ajax({
                url: '/employee/delete/',
                method: 'GET',
                data: {
                    Id: id
                },
                success: function (response) {
                    let row = document.querySelector('#id-' + id)
                    let body = document.querySelector('tbody')
                    body.removeChild(row);
                    console.log('Request succeeded:', response);
                },
                error: function (xhr, status, error) {
                    console.log('Request failed:', xhr.responseText);
                }
            });
        }
    </script>
}